# 部署指南

## 🚀 快速部署

### GitHub Pages 部署（推荐）

当前配置已优化为：
- ✅ **推送到 main 分支时自动构建**
- ✅ **自动部署 out 目录到 GitHub Pages**
- ✅ **支持自定义域名和子路径部署**

---

## 📝 详细步骤

### 1. 推送代码到 GitHub

```bash
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/i.mitkimi.git
git branch -M main
git push -u origin main
```

### 2. 配置 GitHub Pages

1. 进入 GitHub 仓库页面
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**
4. 在 **Source** 下拉菜单中选择 **GitHub Actions**
5. ⚠️ **不需要选择分支和文件夹**，GitHub Actions 会自动处理

### 3. 选择部署方式

#### 方式 A：子路径部署（如 /i.mitkimi）

如果你的仓库名是 `i.mitkimi`，网站将部署在 `https://yourusername.github.io/i.mitkimi/`

**配置步骤**：

编辑 `.github/workflows/deploy.yml`，第 37-39 行取消注释：

```yaml
- name: Build with Next.js
  run: npm run build
  env:
    NODE_ENV: production
    BASE_PATH: /i.mitkimi  # 取消这行注释，改为你的仓库名
```

#### 方式 B：根路径部署 / 自定义域名（推荐）

如果你想使用自定义域名（如 `i.mitkimi.com`）或根路径部署：

**无需修改配置**，默认配置已支持。

**自定义域名额外步骤**：

1. 在 `public` 目录创建 `CNAME` 文件：
```bash
echo "yourdomain.com" > public/CNAME
```

2. 在域名提供商配置 DNS：
```
# A 记录（Apex 域名）
A    @    185.199.108.153
A    @    185.199.109.153
A    @    185.199.110.153
A    @    185.199.111.153

# 或 CNAME 记录（子域名）
CNAME    www    yourusername.github.io.
```

3. 在 GitHub Pages 设置中填写自定义域名

### 4. 自动部署

推送代码到 main 分支后，GitHub Actions 会自动：

1. ✅ 检出最新代码
2. ✅ 安装依赖（npm ci）
3. ✅ 构建项目（生成 out 目录）
4. ✅ 部署 out 目录到 GitHub Pages

你可以在仓库的 **Actions** 标签页查看部署进度。

### 5. 访问网站

部署成功后（约 1-2 分钟），访问：

- **子路径部署**：`https://yourusername.github.io/i.mitkimi/`
- **自定义域名**：`https://yourdomain.com/`
- **用户站点**：`https://yourusername.github.io/`（仓库名必须是 `yourusername.github.io`）

## 本地预览

在推送到 GitHub 之前，你可以在本地预览构建后的静态网站：

```bash
# 构建项目
npm run build

# 预览构建结果
npm run preview
```

然后在浏览器中访问显示的本地地址（通常是 `http://localhost:3000`）。

## 自定义域名（可选）

如果你想使用自定义域名：

1. 在仓库的 `public` 目录下创建 `CNAME` 文件
2. 在文件中写入你的域名，例如：`www.yourdomain.com`
3. 在你的域名提供商处配置 DNS 记录
4. 重新构建和部署

## 故障排查

### 样式或资源加载失败

如果部署后样式或资源无法加载，检查 `next.config.ts` 中的 `basePath` 配置是否与你的仓库名匹配。

### 404 错误

确保在 GitHub Pages 设置中选择了 **GitHub Actions** 作为部署源。

### 构建失败

查看 GitHub Actions 日志，确保所有依赖都已正确安装，没有 TypeScript 或 ESLint 错误。

## 更新网站

要更新网站内容：

1. 修改代码
2. 提交并推送到 `main` 分支
3. GitHub Actions 会自动重新构建和部署

```bash
git add .
git commit -m "Update content"
git push
```

## 环境变量

如果你的项目需要环境变量：

1. 在 GitHub 仓库的 **Settings** > **Secrets and variables** > **Actions** 中添加
2. 在 `.github/workflows/deploy.yml` 中配置环境变量

## 性能优化建议

- ✅ 已启用静态导出（`output: 'export'`）
- ✅ 已禁用图片优化（GitHub Pages 不支持）
- ✅ 使用 Tailwind CSS 进行样式优化
- ✅ 使用 @apron-design/react 组件库

## 技术支持

如有问题，请查看：
- [Next.js 静态导出文档](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

